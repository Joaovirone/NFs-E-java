services:
   postgres:
    image: 'postgres:16-alpine'
    environment:
      - 'POSTGRES_DB=nota_fiscal_db'
      - 'POSTGRES_PASSWORD=${DB_PASSWORD}'
      - 'POSTGRES_USER=${DB_USERNAME}'
    ports:
      - '5001:5432'
